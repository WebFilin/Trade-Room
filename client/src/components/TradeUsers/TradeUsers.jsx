import React from "react";
import styles from "./tradeUsers.module.scss";

function TradeUsers(users) {
  return (
    <secions className={styles.wrapper}>
      <section className={styles.title_page}>Юзеры</section>
    </secions>
  );
}

export default TradeUsers;
